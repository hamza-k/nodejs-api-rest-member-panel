<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>Liste de membres | API REST, NodeJS</title>
  </head>
  <body class="p-5">
    <h1 class="p-5 mb-5 text-center">Liste de tous les membres</h1>
    <table class="table">
            <thead class="thead-dark">
              <tr>
                {% for index, value in membersList[0] %}
                <th scope="col">{{index}}</th>
                {% endfor %}
              </tr>
            </thead>
            <tbody>
              {% for member in membersList %}
              <tr style="cursor: pointer;" onclick="document.location = '/members/{{member.m_id}}'">
                {% for value in member %}
                    <td>{{value}}</td>
                {% endfor %}
              </tr>
              {% endfor %}
            </tbody>
    </table>
    <hr>
    <form action="" method="POST">
      <h2>Add a member</h2>
      <div class="form-group">
          <label for="m_name">Name</label>
          <input type="text" class="form-control" id="m_name" name="m_name">
      </div>
      <button type="submit" class="btn btn-primary">Add</button>
    </form>


    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
  </body>
</html>